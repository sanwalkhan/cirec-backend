{
    "version": 2,
    "builds": [
      {
        "src": "api/**/*.ts",
        "use": "@vercel/node",
        "config": {
          "includeFiles": ["tsconfig.json"]
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/api/$1"
      }
    ]
  }
  